apiVersion: v1
kind: Service
metadata:
  name: cast-db-{{ .Release.Namespace }}
  # namespace: {{ .Release.Namespace }}
  labels:
    app: cast-{{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: postgres-cast-{{ .Release.Namespace }}
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
