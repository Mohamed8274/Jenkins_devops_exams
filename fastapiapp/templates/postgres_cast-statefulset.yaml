apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres_cast-{{ .Release.Namespace }}
  labels:
    app: cast-{{ .Release.Namespace }}
spec:
  serviceName: cast_db-{{ .Release.Namespace }}
  replicas: 1
  selector:
    matchLabels:
      app: postgres_cast-{{ .Release.Namespace }}
  template:
    metadata:
      labels:
        app: postgres_cast-{{ .Release.Namespace }}
    spec:
      containers:
      - name: postgres_cast-{{ .Release.Namespace }}
        image: postgres:12.1-alpine
        ports:
        - containerPort: 5432
          protocol: TCP
        envFrom:
        - configMapRef:
            name: postgres_cast-configmap-{{ .Release.Namespace }}
        - secretRef:
            name: postgres_cast-secret-{{ .Release.Namespace }}
        volumeMounts:
        - name: postgres_data_cast
          mountPath: /var/lib/postgresql/data/
      volumes:
      - name: postgres_data_cast
        persistentVolumeClaim:
          claimName: postgres_cast-pvc-{{ .Release.Namespace }}